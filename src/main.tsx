import React from 'react'
import ReactDOM from 'react-dom/client'
import AuditVisualization from './AuditVisualization.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AuditVisualization />
  </React.StrictMode>,
)